#JSGF V1.0;

/**
 * JSGF Grammar for standard commands
 */

grammar commands;

public <standard_commands> =
                      padėk                   { help } |
                      ([eik į pagrindinį]|pagrindinis) meniu    { goto_menu } ;
